# Dataset Card Pointer

The current dataset card is maintained at the repository root:

- `DATASET_CARD.md`

This file is kept as a compatibility pointer because earlier project documentation linked to `docs/dataset_card.md`.

For clinicians only — supports review, not diagnosis or prescribing.
