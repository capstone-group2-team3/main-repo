# Model Card Pointer

The current model card is maintained at the repository root:

- `MODEL_CARD.md`

This file is kept as a compatibility pointer because earlier project documentation linked to `docs/severity_model_card.md`.

For clinicians only — supports review, not diagnosis or prescribing.
